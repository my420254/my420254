# 对外展示总览

这份文档的用途很明确：把你这几年做的工作压成一条清晰的主线，方便你自己回忆，也方便后续改成简历、主页和面试讲述稿。

## 你的岗位定位

智能体研发，偏具身规划、大模型应用架构、数据治理和系统工程。

## 一句话主线

你不是只做了单个模型，也不是只做了单个框架，而是把“数据清洗 -> 监督微调 -> 论文方法验证 -> 智能体运行时 -> benchmark 接入 -> 外部命令入口”这条链路真正打通了。

## 公开项目矩阵

| 名称 | 类型 | 状态 | 你可以强调的点 |
| --- | --- | --- | --- |
| Hy-MoRA | 长尾情感分析 PEFT 论文 | WISA 已接收 | 110M 级模型对标 7B LLM，长尾感知和部署效率同时成立 |
| HiPro-LoRA | 低资源情感分析 PEFT 论文 | ECML-PKDD 已接收 | 严格 held-out 协议，验证集只做选择，测试集只做最终报告 |
| SPEAR | ECPE 论文 | KBS 投稿版本 | 结构解耦、DoRA biaffine、R-Drop、窗口约束解码 |
| FASTE | ASTE 论文 | ESWA 投稿版本 | 多层特征融合 + 对抗训练，兼顾边界对齐和实时性 |
| OurAgent | 智能体框架 | 持续迭代 | LangGraph + ROS + CommandBus + 中断恢复 + 反思闭环 |

## 1. Hy-MoRA

### 解决的问题

中文社交媒体长尾情感分析里，少数类特征容易退化，常规 LoRA 在低秩约束下很难把尾部类别拉开。

### 你做了什么

- 设计了层次化语义聚合模块，缓解 `[CLS]` 单点表征不够用的问题。
- 设计了尾部感知记忆对齐模块，用原型记忆和尾部加权对比目标强化少数类边界。
- 把部署阶段和训练阶段解耦，确保推理时没有额外延迟。

### 亮点

- 110M 参数级别就能和 7B LLM 做有效对比。
- 在 SMP2020-EWECT 上，Macro-F1 相比 Qwen-2.5 7B 基线提升 6.12 个百分点。
- 推理吞吐量约提升 55 倍，适合高并发舆情场景。

### 对外讲法

“我做的是一个能落地的长尾感知 PEFT 框架，不只是把 LoRA 调一调，而是把表示瓶颈和尾部边界问题一起处理掉。”

## 2. HiPro-LoRA

### 解决的问题

低资源、长尾分布下，LoRA 容易出现 minority class 表征退化，特别是在严格 held-out 设置下，很多方法一到最终测试就掉点。

### 你做了什么

- 设计了层次化池化模块，补足单纯 `[CLS]` 的信息损失。
- 设计了 prototype memory regularization，让尾部类别的几何边界更稳定。
- 用严格 held-out 协议重新组织实验，避免把验证集选择和最终报告混在一起。

### 亮点

- 在 6 组 held-out 配置里，Tail-F1 有 5 组最优，Macro-F1 有 4 组最优。
- 在 SMP2020、SST-5、TweetEval 三个数据上都能保持稳定优势。
- 对比的是严格 few-shot / ICL 设定下的大模型结果，结论更硬。

### 对外讲法

“我不是只做了一个更大的模型，而是把低资源场景里的表征退化问题拆开、测透、再修好。”

## 3. SPEAR / ECPE

### 解决的问题

情感-原因对提取本质上是一个高精度结构抽取任务，现有方法容易在长距离噪声和过拟合上出问题。

### 你做了什么

- 做了跨度感知的 clause 表征，补足纯 CLS 表示的粗糙性。
- 引入 DoRA biaffine scoring，提升解耦打分的稳定性。
- 用 R-Drop 和窗口约束解码抑制过置信预测和远距离假阳性。

### 亮点

- 严格 10 折交叉验证下达到 77.24% F1。
- 比此前最优基线提升 0.87 个百分点。
- Precision 提升幅度更明显，说明模型不是只“追召回”，而是结构更稳。

### 对外讲法

“我把 ECPE 从单纯堆模型，推进到更讲结构归纳偏置的抽取框架，强调高精度和可解释的边界约束。”

## 4. FASTE / ASTE

### 解决的问题

ASTE 里很多方法只看最后一层，边界信息和局部语法很容易丢，导致 triplet 边界错位。

### 你做了什么

- 做多层特征融合，把中间层语法信息拉回来。
- 加入对抗训练，平滑高噪文本下的损失地形。
- 保持非生成式抽取路线，兼顾边界精度和实时性。

### 亮点

- 在 ASTE-Data-V2 上达到非生成式系统里的强结果。
- 14lap 上 F1 到 66.60。
- 模型体量和吞吐量都适合工程部署，而不是只适合论文图表。

### 对外讲法

“我做的是一个面向真实应用的 ASTE 框架，不是泛泛的情感分析 baseline。”

## 5. OurAgent

### 解决的问题

把模型真正放进智能体 runtime 之后，问题不再是“能不能预测”，而是：

- 外部指令怎么进来
- 新任务来了怎么插单
- 旧任务怎么挂起和恢复
- 失败后怎么分层反思
- 多 benchmark 怎么统一跑

### 你做了什么

- 用 LangGraph 拆成 Understanding / Planning / Task Management / Reflection 四阶段。
- 做 CommandBus，把 ROS 文本、CLI 命令和外部输入统一成结构化任务流。
- 设计任务栈式中断与恢复，支持多层嵌套插单。
- 把 benchmark、执行器和本地大模型调用解耦，方便并行跑多个环境。

### 亮点

- 这不是脚本集合，而是一个有运行时边界的 agent framework。
- 你已经把“模型能力”变成“能接任务、能恢复、能追踪、能复盘”的系统能力。
- 对外讲的时候，这一块最能体现你是系统型智能体研发，不是只会调 prompt。

### 对外讲法

“我把一个规划模型接进了可中断、可恢复、可反思的智能体系统，真正做到了训练、部署、评估、调度一体化。”

## GitHub / Gitee 展示建议

### GitHub 主页

主页只放三件事：

1. 你是谁，做什么
2. 你最强的 4 篇论文和 1 个系统框架
3. 你的公开链接和联系方式

### 仓库 README

每个仓库都按同一个模板写：

1. 一句话问题定义
2. 方法框图或模块说明
3. 核心结果
4. 运行入口
5. 论文状态

### Gitee 镜像

Gitee 只负责国内可访问，不要和 GitHub 两套内容分裂。

## 后续还能继续补的展示方向

- RAG：检索、切片、召回、重排、答案生成的一体化项目
- Agent：工具调用、任务分解、长短期记忆、失败恢复
- LLM Ops：并行调度、推理服务、量化、吞吐和延迟优化
- 数据方向：清洗、对齐、抽样、评测集构造、偏差分析

你后面如果继续做这些内容，GitHub 主页就不需要重新装修，只要往同一套结构里加新卡片即可。

## 公共口径边界

- 可以写：已接收、投稿中、最终版已提交、核心结果、系统能力。
- 不建议先写：未经官方确认的录取率、夸大的排名、无法复现的绝对速度数字。
- 需要写录取率时，补官方来源后再写。
